clc;
close all;
clear;

omega = linspace(0, 0.5, 200)
m = 0:99;
filename = strcat('ЧХ.txt')
addpath(genpath('Пункт 1'))

data = importdata(filename);

plot(omegadata(:,2),'-o', 'MarkerFaceColor','blue')
grid('on')

title('аргумент ДВРФ выходного сигнала')
ylabel('arg{S(\Omega)}')
xlabel('Безразмерная частота \Omega')
print(['ЧХ фильтра'],'-dpng')