clc;
close all;
clear;

omega = linspace(0, 0.5, 85)
m = 0:99;
filename = strcat('DVRF.txt')
addpath(genpath('Пункт 1'))

data = importdata(filename);

plot(omega, data(:, 3))
grid('on')

title('м ДВРФ входного сигнала')
ylabel('S(\Omega)')
xlabel('Безразмерная частота')
print(['модуль выходного сигнала ДВРФ'],'-dpng')